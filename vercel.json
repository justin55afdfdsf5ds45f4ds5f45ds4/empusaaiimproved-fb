{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/panda/:path*", "destination": "/panda/:path*" },
    { "source": "/((?!panda).*)", "destination": "/index.html" }
  ]
}
